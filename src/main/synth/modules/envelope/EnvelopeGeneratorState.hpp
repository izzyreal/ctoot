#pragma once

namespace ctoot {
	namespace synth {
		namespace modules {
			namespace envelope {

				enum EnvelopeGeneratorState { DELAY, ATTACK, HOLD, DECAY, SUSTAIN, RELEASE, COMPLETE };

			}
		}
	}
}
